import { DevStudio } from "./DevStudio";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Modo Desenvolvedor · V7M Lab",
};

export default function DevPreviewPage() {
  return <DevStudio />;
}
