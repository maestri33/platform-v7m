---
/**
 * Barra de progresso de leitura via Design System @v7m/ui.
 * Executa em CSS puro acelerado por hardware (animation-timeline: scroll())
 * com 0ms de custo de JS na thread principal.
 */
import { ReadingProgressBar } from '@v7m/ui';
---

<ReadingProgressBar brand="supletivo" size="md" glow={true} />
