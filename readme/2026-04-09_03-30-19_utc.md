# Atualizações recentes

- Data e hora: 2026-04-09 03:30:19 UTC

## O que foi atualizado nesta rodada

- criado `requirements.txt` com as dependências congeladas da `.venv` atual
- criado `.env.example` para servir como template de ambiente sem segredos
- mantido o snapshot técnico anterior em `readme/2026-04-09_03-11-46_utc.md`
- simplificado o `README.md` da raiz para apontar apenas para a pasta `readme/`

## Correções de admin

- revisados os `admin.py` principais de `authentication`, `profiles`, `visitors` e `notifications`
- corrigido o uso de `format_html()` sem `args/kwargs`, que quebrava o admin no Django 6
- padronizados badges e labels visuais para evitar novos erros do mesmo tipo
- substituído link hardcoded do admin de `profiles` por `reverse()`

## Ajuste de testes

- ajustado o teste de OTP em `apps/authentication/tests.py` para respeitar o `transaction.on_commit`

## Validação executada

- `python manage.py check`: ok
- `python manage.py test -v 1`: ok
- `python manage.py test notifications.tests apps.authentication.tests -v 1`: ok
